// Redis config

/*

import { REDIS } from '../configs';
import * as Redis from 'ioredis';

const redis = new Redis(REDIS.PORT, REDIS.HOST);

/!* Listen redis error *!/
redis.on('error', () => {});

export { redis };

*/
